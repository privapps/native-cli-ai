---
description: Rules for writing and maintaining project documentation.
globs: "docs/**/*.md,README*,.cursor/rules/**"
alwaysApply: false
---

# Documentation Standards

## Structure

- `docs/product/prd.md` is the source of truth for product scope, personas, and MVP definition.
- `docs/reference/tech-stack.md` records every dependency choice with rationale and rejected alternatives.
- `docs/reference/architecture.md` defines crate boundaries, data flows, IPC protocol, and security model.
- `docs/README.md` is the documentation hub; the root `README.md` remains the quick-start guide.
- User-facing behavior belongs in `docs/user/`; plans and specifications belong in `docs/design/`.

## Writing Style

- Use plain, direct language. Avoid marketing copy.
- Use tables for comparisons and structured data.
- Use mermaid diagrams for architecture, data flows, and state machines.
- Keep each document under 500 lines. Split into separate files if it grows beyond that.

## Consistency

- When a dependency is added or removed, update `docs/reference/tech-stack.md` in the same commit.
- When a crate boundary changes, update `docs/reference/architecture.md` in the same commit.
- When MVP scope changes, update `docs/product/prd.md` in the same commit.
- Cursor rules in `.cursor/rules/` must stay in sync with architectural decisions in the docs.

## Versioning

- Docs are version-controlled alongside code.
- `.nca/config.local.toml` and `.nca/sessions/` are gitignored. Never document them as committed files.
- `.ncarc` (project instructions) is version-controlled and documented.
